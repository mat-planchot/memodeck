<% 
Date.prototype.addDays = function(days) {
    var date = new Date(this.valueOf());
    date.setDate(date.getDate() + days);
    return date;
}

var date = new Date();
date.addDays(5).toISOString().replace(/T.+/,'')

params.nbreview = params.nbreview + 1;
params.difficulty = params.difficulty + 0.25;
params.nbdayreview = Math.round(params.nbdayreview * params.difficulty);
%>